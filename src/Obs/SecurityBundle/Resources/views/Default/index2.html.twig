Hello Utilizador Localizado! Página 2 do administrador

{% if app.user %}
    Username: {{ app.user.username }}
    <a href="{{ path('logout') }}">Logout</a>
{% else %}
    <a href="{{ path('login') }}">Login</a>
{% endif %}