<header>
    <h3>Roles list</h3>
</header>

<h1></h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Role</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('roles_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.role }}</td>
            <td>
            <ul>
                <li>
                    <a href="{{ path('roles_show', { 'id': entity.id }) }}">show</a>
                </li>
                <li>
                    <a href="{{ path('roles_edit', { 'id': entity.id }) }}">edit</a>
                </li>
            </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('roles_new') }}">
            Create a new entry
        </a>
    </li>
</ul>