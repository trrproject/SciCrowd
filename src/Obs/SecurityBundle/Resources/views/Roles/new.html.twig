 <header>
     <h3>Roles creation</h3>
 </header>

<form action="{{ path('roles_create') }} " method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('roles') }}">
            Back to the list
        </a>
    </li>
</ul>